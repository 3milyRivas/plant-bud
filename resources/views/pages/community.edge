@layout()

@slot('title')
<title>Community</title>
@endslot

@let(errors = formErrors || {})

<div class="min-h-screen bg-[#6C8E6B] text-[#113E14]" data-community-page>
  <div class="fixed left-0 top-0 z-50 w-full px-4 py-4 lg:px-8">
    <div class="mx-auto flex max-w-[1240px] items-center gap-3 rounded-[2rem] border border-white/30 bg-white/24 px-4 py-3 shadow-[0_18px_55px_rgba(17,62,20,0.18)] backdrop-blur-2xl sm:rounded-full sm:px-5">
      <a href="/homepage" class="h-11 w-11 flex-none rounded-full bg-[#113E14] bg-[url('/resources/images/essential/logo.png')] bg-contain bg-center bg-no-repeat shadow-md transition hover:scale-105" aria-label="Plant Bud home"></a>

      @form({
        action: '/community/search',
        method: 'GET',
        class: 'hidden flex-none items-center gap-3 md:flex'
      })
        <div class="w-[250px] rounded-full bg-white/78 px-5 py-2 shadow-inner">
          <input name="username" type="text" placeholder="Search username" class="w-full bg-transparent text-sm font-semibold text-[#113E14] outline-none placeholder:text-[#6B8C6C]/70">
        </div>

        <button type="submit" class="flex h-10 w-10 items-center justify-center rounded-full bg-[#113E14] text-white shadow-md transition hover:-translate-y-0.5 hover:bg-[#1b5420]" aria-label="Search community">
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35m1.85-5.65a7.5 7.5 0 11-15 0 7.5 7.5 0 0115 0z" />
          </svg>
        </button>
      @end

      <div class="flex min-w-0 flex-1 items-center justify-center gap-2">
        <div class="h-8 w-8 flex-none bg-[url('/resources/img/img3.png')] bg-contain bg-no-repeat"></div>
        <span class="truncate text-base font-black text-[#113E14] sm:text-lg">Community</span>
        <div class="h-8 w-8 flex-none -scale-x-100 bg-[url('/resources/img/img3.png')] bg-contain bg-no-repeat"></div>
      </div>

      <nav class="flex flex-none items-center gap-2">
        <a href="/homepage" class="hidden items-center gap-2 rounded-2xl bg-white px-4 py-2 text-sm font-black text-[#113E14] shadow-md transition hover:-translate-y-0.5 hover:bg-[#EDE7D6] sm:flex">
          <div class="h-5 w-5 bg-[url('/resources/img/img4.png')] bg-contain bg-no-repeat"></div>
          Home
        </a>
        <a href="/notification" class="hidden h-9 w-9 rounded-xl bg-[url('/resources/img/campana.png')] bg-contain bg-center bg-no-repeat transition hover:-translate-y-0.5 sm:block" aria-label="Notifications"></a>
        <a href="/favorites" class="h-9 w-9 rounded-xl bg-[url('/resources/img/fav.png')] bg-contain bg-center bg-no-repeat transition hover:-translate-y-0.5" aria-label="Saved posts"></a>
      </nav>

      <a href="/profile" class="hidden max-w-[250px] flex-none items-center gap-3 rounded-full bg-white/18 py-1 pl-1 pr-4 transition hover:bg-white/28 lg:flex">
        @if(viewer.avatarUrl)
          <img src="{{ viewer.avatarUrl }}" alt="" class="h-10 w-10 flex-none rounded-full object-cover">
        @else
          <span class="flex h-10 w-10 flex-none items-center justify-center rounded-full bg-[#113E14] text-sm font-black text-[#EDE7D6]">
            {{ viewer.initial }}
          </span>
        @endif
        <span class="min-w-0">
          <span class="block truncate text-sm font-black text-[#113E14]">{{ viewer.displayName }}</span>
          <span class="block truncate text-xs font-bold text-blue-700">{{ '@' + viewer.username }}</span>
        </span>
      </a>
    </div>

    @if(searchError)
      <div class="mx-auto mt-3 max-w-[1240px] rounded-2xl border border-red-200/40 bg-red-950/30 px-5 py-3 text-sm font-bold text-red-100 backdrop-blur-xl">
        {{ searchError }}
      </div>
    @endif
  </div>


  <div class="flex gap-6 bg-[#6C8E6B]/80 px-4 pt-28 py-10 lg:px-6">
    <div class="hidden w-64 flex-none space-y-6 lg:block">
      <div class="sticky top-40 overflow-hidden rounded-3xl border border-white/30 bg-white/22 px-3 pb-4 pt-3 shadow-xl backdrop-blur-xl">
        <div class="relative h-40 overflow-hidden rounded-2xl bg-[#113E14]/15">
          @if(viewer.bannerUrl)
            <img src="{{ viewer.bannerUrl }}" alt="" class="h-full w-full object-cover">
          @else
            <img src="/resources/img/flo.jpg" alt="" class="h-full w-full object-cover">
          @endif
          <div class="absolute inset-0 bg-gradient-to-t from-[#113E14]/45 to-transparent"></div>
        </div>

        <div class="-mt-12 flex justify-center">
          @if(viewer.avatarUrl)
            <img src="{{ viewer.avatarUrl }}" alt="" class="relative h-24 w-24 rounded-full border-4 border-white object-cover shadow-lg">
          @else
            <span class="relative flex h-24 w-24 items-center justify-center rounded-full border-4 border-white bg-[#113E14] text-3xl font-black text-[#EDE7D6] shadow-lg">
              {{ viewer.initial }}
            </span>
          @endif
        </div>

        <div class="mt-3 flex flex-col items-center px-2 text-center">
          <h2 class="max-w-full truncate text-lg font-black text-black">{{ viewer.displayName }}</h2>
          <p class="max-w-full truncate text-sm font-bold text-black/38">{{ '@' + viewer.username }}</p>
          <span class="mt-3 rounded-full bg-[#EDE7D6] px-3 py-1 text-[11px] font-black uppercase tracking-[0.12em] text-[#113E14]">
            {{ viewer.roleLabel }}
          </span>

          <div class="mt-5 grid w-full grid-cols-3 gap-2 text-center">
            <div class="rounded-2xl bg-white/30 p-3">
              <p class="font-black text-black">{{ viewer.stats.posts }}</p>
              <p class="text-[11px] font-bold text-black/38">Posts</p>
            </div>
            <div class="rounded-2xl bg-white/30 p-3">
              <p class="font-black text-black">{{ viewer.stats.followers }}</p>
              <p class="text-[11px] font-bold text-black/38">Followers</p>
            </div>
            <div class="rounded-2xl bg-white/30 p-3">
              <p class="font-black text-black">{{ viewer.stats.following }}</p>
              <p class="text-[11px] font-bold text-black/38">Follows</p>
            </div>
          </div>

          <a href="/profile" class="mt-4 w-full rounded-2xl bg-[#113E14] px-5 py-2.5 text-sm font-black text-white shadow-md transition hover:-translate-y-0.5 hover:bg-[#1b5420]">
            View profile
          </a>
        </div>
      </div>
    </div>

    <main class="min-w-0 flex-1 space-y-6">
      <div class="rounded-3xl border border-white/30 bg-white/24 p-4 shadow-xl backdrop-blur-xl">
        @form({
          action: '/community/posts',
          method: 'POST',
          enctype: 'multipart/form-data',
          class: 'space-y-4',
          'data-community-post-form': true
        })
          <div class="flex items-start gap-4 sm:gap-6">
            @if(viewer.avatarUrl)
              <img src="{{ viewer.avatarUrl }}" alt="" class="h-12 w-12 rounded-full object-cover">
            @else
              <span class="flex h-12 w-12 flex-none items-center justify-center rounded-full bg-[#113E14] font-black text-[#EDE7D6]">
                {{ viewer.initial }}
              </span>
            @endif

            <div class="min-w-0 flex-1 rounded-2xl bg-[#FBFBFB] px-4 py-3 shadow-sm">
              <textarea name="body" maxlength="2000" rows="2" placeholder="Share something..." class="min-h-16 w-full resize-none bg-transparent text-sm font-semibold text-gray-700 outline-none placeholder:text-gray-500">{{ old.body ?? '' }}</textarea>
            </div>
          </div>

          @if(errors.body)
            <p class="text-xs font-bold text-red-800">{{ errors.body[0] }}</p>
          @endif
          <p class="hidden text-xs font-bold text-red-800" data-community-form-error></p>

          <details class="rounded-2xl bg-white/22 px-4 py-3" data-community-options>
            <summary class="cursor-pointer text-sm font-black text-[#113E14]">Post details</summary>
            <div class="mt-4 grid gap-4 md:grid-cols-2">
              <label class="block">
                <span class="text-xs font-black uppercase tracking-[0.12em] text-black/50">Image</span>
                <input type="file" name="image" accept="image/jpeg,image/png,image/webp" class="mt-2 block w-full rounded-xl bg-white/75 px-3 py-2 text-sm font-semibold" data-community-image-input>
                @if(errors.image)
                  <span class="mt-2 block text-xs font-bold text-red-800">{{ errors.image[0] }}</span>
                @endif
              </label>
              <label class="block">
                <span class="text-xs font-black uppercase tracking-[0.12em] text-black/50">Hashtags</span>
                <input name="hashtags" value="{{ old.hashtags ?? '' }}" maxlength="240" placeholder="plants balcony care" class="mt-2 h-11 w-full rounded-xl bg-white/75 px-3 text-sm font-semibold outline-none">
              </label>
              <label class="block">
                <span class="text-xs font-black uppercase tracking-[0.12em] text-black/50">Poll question</span>
                <input name="poll_question" value="{{ old.poll_question ?? '' }}" maxlength="160" placeholder="Which plant should I choose?" class="mt-2 h-11 w-full rounded-xl bg-white/75 px-3 text-sm font-semibold outline-none" data-community-poll-input>
              </label>
              <label class="block">
                <span class="text-xs font-black uppercase tracking-[0.12em] text-black/50">Poll options</span>
                <textarea name="poll_options" maxlength="600" rows="3" placeholder="Monstera&#10;Pothos&#10;Snake plant" class="mt-2 w-full resize-none rounded-xl bg-white/75 px-3 py-2 text-sm font-semibold outline-none">{{ old.poll_options ?? '' }}</textarea>
                @if(errors.poll_options)
                  <span class="mt-2 block text-xs font-bold text-red-800">{{ errors.poll_options[0] }}</span>
                @endif
              </label>
            </div>
          </details>

          <div class="flex flex-wrap items-center justify-between gap-3">
            <div class="flex flex-wrap items-center gap-2">
              <button type="button" class="flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-bold text-black/70 transition hover:bg-white/35 hover:text-[#113E14]" data-community-file-trigger>
                <div class="h-6 w-6 bg-[url('/resources/img/imagen.png')] bg-contain bg-no-repeat"></div>
                Images
              </button>

              <button type="button" class="flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-bold text-black/70 transition hover:bg-white/35 hover:text-[#113E14]" data-community-poll-trigger>
                <div class="h-6 w-6 bg-[url('/resources/img/pol.png')] bg-contain bg-no-repeat"></div>
                Poll
              </button>

              <span class="hidden max-w-[12rem] truncate rounded-full bg-white/45 px-3 py-1.5 text-xs font-bold text-[#113E14]" data-community-file-name></span>
            </div>

            <div class="flex items-center gap-2">
              <select name="visibility" class="h-10 rounded-xl bg-white/65 px-3 text-sm font-black text-[#113E14] outline-none">
                <option value="public">Public</option>
                <option value="private">Private</option>
              </select>

              <button type="submit" class="flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-black text-[#113E14] transition hover:bg-[#EDE7D6] hover:shadow-md active:translate-y-0.5">
                <div class="h-6 w-6 bg-[url('/resources/img/send.png')] bg-contain bg-no-repeat"></div>
                Publish
              </button>
            </div>
          </div>
        @end
      </div>

      <div class="flex items-center justify-between gap-4">
        <div class="h-0.5 flex-1 bg-[url('/resources/img/line.png')] bg-cover bg-center"></div>
        <div class="flex items-center gap-2 rounded-full bg-white/18 px-3 py-1.5">
          <span class="text-xs font-black uppercase tracking-[0.12em] text-black/38">Sort by</span>
          <span class="text-sm font-black text-black">Recent</span>
          <div class="h-7 w-7 bg-[url('/resources/img/row.png')] bg-contain bg-no-repeat"></div>
        </div>
      </div>

      <div class="space-y-4" data-community-feed>
        @each(post in posts)
          @!component('components/community-post-card', { post, viewer })
        @else
          <div class="rounded-3xl border border-white/30 bg-white/22 p-8 text-center shadow-lg backdrop-blur-xl">
            <h2 class="text-2xl font-black text-black">No posts yet</h2>
            <p class="mt-2 text-sm font-bold text-black/45">Share the first plant update.</p>
          </div>
        @end
      </div>
    </main>

    <aside class="sticky top-28 hidden max-h-[calc(100vh-8rem)] w-[300px] flex-none self-start space-y-4 overflow-y-auto pr-1 [scrollbar-width:none] xl:block [&::-webkit-scrollbar]:hidden">
      <div class="space-y-4 rounded-3xl border border-white/30 bg-white/22 p-4 shadow-xl backdrop-blur-xl">
        <div class="flex items-center justify-between">
          <h2 class="text-lg font-black text-black">Activity</h2>
          <span class="rounded-full bg-[#EDE7D6] px-2.5 py-1 text-[10px] font-black uppercase tracking-[0.14em] text-[#113E14]">live</span>
        </div>

        @each(post in posts.slice(0, 4))
          <a href="#post-{{ post.id }}" class="flex items-center justify-between gap-3 rounded-2xl bg-white/18 p-2 transition hover:-translate-y-0.5 hover:bg-white/30">
            <div class="flex min-w-0 items-center gap-2">
              @if(post.author.avatarUrl)
                <img src="{{ post.author.avatarUrl }}" alt="" class="h-10 w-10 rounded-full object-cover">
              @else
                <span class="flex h-10 w-10 rounded-full bg-[#113E14] text-xs font-black text-[#EDE7D6] items-center justify-center">{{ post.author.initial }}</span>
              @endif
              <div class="min-w-0 leading-tight">
                <p class="truncate text-xs font-black text-black">{{ post.author.displayName }}</p>
                <p class="truncate text-[11px] font-bold text-black/42">{{ post.author.roleLabel }} · {{ post.createdAtHuman }}</p>
              </div>
            </div>
            @if(post.mediaUrl)
              <img src="{{ post.mediaUrl }}" alt="" class="h-10 w-10 rounded-xl object-cover">
            @endif
          </a>
        @else
          <p class="text-xs font-semibold text-black/45">Activity will appear here.</p>
        @end
      </div>

      <div class="space-y-3 rounded-3xl border border-white/30 bg-white/22 p-4 shadow-xl backdrop-blur-xl">
        <div class="flex items-center justify-between">
          <h2 class="text-sm font-black text-black">Suggestions for you</h2>
          <span class="text-xs font-bold text-black/42">new</span>
        </div>

        @each(user in suggestions)
          <div class="flex items-center justify-between gap-3">
            <a href="/users/{{ user.username }}" class="flex min-w-0 items-center gap-3">
              @if(user.avatarUrl)
                <img src="{{ user.avatarUrl }}" alt="" class="h-11 w-11 rounded-full object-cover">
              @else
                <span class="flex h-11 w-11 flex-none items-center justify-center rounded-full bg-[#113E14] font-black text-[#EDE7D6]">{{ user.initial }}</span>
              @endif

              <span class="min-w-0 leading-tight">
                <span class="block truncate text-sm font-black text-black">{{ user.displayName }}</span>
                <span class="block truncate text-[11px] font-bold text-gray-500">{{ user.roleLabel }} · {{ '@' + user.username }}</span>
              </span>
            </a>

            @form({
              action: '/users/' + user.username + '/follow',
              method: 'POST',
              class: 'shrink-0',
              'data-follow-form': true
            })
              <button type="submit" class="rounded-full bg-white/45 px-3 py-1.5 text-xs font-black text-blue-600 transition hover:bg-[#EDE7D6]" data-follow-label>follow</button>
            @end
          </div>
        @else
          <p class="text-xs font-semibold text-black/45">No suggestions right now.</p>
        @end
      </div>
    </aside>
  </div>
</div>

@end
